# Agent Skill Suite Adapter

Follow the nearest `AGENTS.md` file in the repository.

Treat `bundle.yaml` as the canonical skill inventory. Start with `skills/suite-orchestrator/SKILL.md` for multi-stage work, then load only the specialist skills it routes. Preserve the suite Task Packet and Handoff Packet across every stage.
