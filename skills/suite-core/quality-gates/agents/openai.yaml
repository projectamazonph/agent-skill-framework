interface:
  display_name: Quality Gates
  short_description: Verify evidence before engineering work completes
  default_prompt: Use $quality-gates to verify this change against all
    required completion evidence.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
